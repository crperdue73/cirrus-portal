{
  "_comment": "Cirrus Portal secrets — chmod 600, never commit, never back up, never ship. gatewayTokens is keyed by gateway id from portal-config.json.",
  "gatewayTokens": {
    "home": "REPLACE_WITH_GATEWAY_TOKEN"
  },
  "portalPassword": "REPLACE_WITH_A_STRONG_UNIQUE_ADMIN_PASSWORD"
}
