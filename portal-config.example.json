{
  "_comment": "portal-config.json is TOKEN-FREE. Gateway tokens + the bootstrap admin password live in portal-secrets.json (0600). TLS/exposure: a public bind requires TLS (plan item 5) — see deploy/Caddyfile and deploy/nginx/cirrus-portal.conf.",
  "port": 18800,
  "bind": "127.0.0.1",
  "gateways": [
    {
      "id": "home",
      "name": "Home",
      "url": "ws://127.0.0.1:18790",
      "enabled": true
    }
  ],
  "tlsMode": "auto",
  "trustProxy": true,
  "sessionTtlHours": 12
}
