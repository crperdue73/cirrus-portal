# ── Runtime state & secrets (never commit) ────────────────────────────────
portal-config.json
portal-config.json.bak*
portal-config.json.*
portal-secrets.json
portal-secrets.json.tmp
portal-device.json
portal-users.json
portal-rooms.json
portal-context.json
portal-audit.log
portal.log

# ── Local env / editor cruft ───────────────────────────────────────────────
.env
.env.*
*.log
*.swp
.DS_Store

# ── Installer / release artifacts ──────────────────────────────────────────
install.log
portal-credentials.txt
portal-first-run.txt
backups/
dist/
